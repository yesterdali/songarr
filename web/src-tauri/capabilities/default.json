{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window (core APIs only — the app needs no native plugins; downloads use the webview's IndexedDB).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
